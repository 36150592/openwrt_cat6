Index: odhcp6c-2015-07-13/src/odhcp6c.c
===================================================================
--- odhcp6c-2015-07-13.orig/src/odhcp6c.c
+++ odhcp6c-2015-07-13/src/odhcp6c.c
@@ -284,8 +284,15 @@ int main(_unused int argc, char* const a
 
 		signal_usr1 = signal_usr2 = false;
 		int mode = dhcpv6_set_ia_mode(ia_na_mode, ia_pd_mode);
-		if (mode != DHCPV6_STATELESS)
-			mode = dhcpv6_request(DHCPV6_MSG_SOLICIT);
+		//if (mode != DHCPV6_STATELESS)
+		//	mode = dhcpv6_request(DHCPV6_MSG_SOLICIT);
+		if (mode != DHCPV6_STATELESS) {
+			if (strcmp(LTE_IFNAME_1, ifname) == 0 ) {
+				mode = dhcpv6_request(DHCPV6_MSG_INFO_REQ);
+			} else {
+				mode = dhcpv6_request(DHCPV6_MSG_SOLICIT);
+			}
+		}
 		odhcp6c_signal_process();
 
 		if (mode < 0)
Index: odhcp6c-2015-07-13/src/odhcp6c.h
===================================================================
--- odhcp6c-2015-07-13.orig/src/odhcp6c.h
+++ odhcp6c-2015-07-13/src/odhcp6c.h
@@ -33,6 +33,8 @@
 
 #define DEFAULT_MIN_UPDATE_INTERVAL 30
 
+#define LTE_IFNAME_1 "bmwan0"
+
 enum dhcvp6_opt {
 	DHCPV6_OPT_CLIENTID = 1,
 	DHCPV6_OPT_SERVERID = 2,
Index: odhcp6c-2015-07-13/src/ra.c
===================================================================
--- odhcp6c-2015-07-13.orig/src/ra.c
+++ odhcp6c-2015-07-13/src/ra.c
@@ -401,6 +401,9 @@ bool ra_process(void)
 				entry->target.s6_addr32[3] = lladdr.s6_addr32[3];
 
 				changed |= odhcp6c_update_entry(STATE_RA_PREFIX, entry, 7200, true);
+				if (strcmp(LTE_IFNAME_1, if_name) == 0) {
+					changed |= odhcp6c_update_entry(STATE_IA_PD, entry, 0, false);
+				}
 			} else if (opt->type == ND_OPT_RECURSIVE_DNS && opt->len > 2) {
 				entry->router = from.sin6_addr;
 				entry->priority = 0;
Index: odhcp6c-2015-07-13/src/script.c
===================================================================
--- odhcp6c-2015-07-13.orig/src/script.c
+++ odhcp6c-2015-07-13/src/script.c
@@ -386,7 +386,7 @@ void script_call(const char *status)
 		s46_to_env(STATE_S46_LW, s46_lw, s46_lw_len);
 		bin_to_env(custom, custom_len);
 
-		if (odhcp6c_is_bound()) {
+		if (odhcp6c_is_bound() || (strcmp(LTE_IFNAME_1, argv[1]) == 0)) {
 			entry_to_env("PREFIXES", prefix, prefix_len, ENTRY_PREFIX);
 			entry_to_env("ADDRESSES", address, address_len, ENTRY_ADDRESS);
 		}
